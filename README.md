# CableTelco / TV MAX

Este paquete quedó enroutado al proyecto Supabase destino `mqsocgbgebnkckjdbwdz`.

Destino:
- URL: https://mqsocgbgebnkckjdbwdz.supabase.co
- Publicable key: configurada en `js/app.js`

El backend destino ya contiene el esquema de `perfiles`, `ventas`, `configuracion` y `encuestas`, RLS/policies, la RPC `dashboard_tvmax_mensual`, Storage `app-assets` y la Edge Function `admin-users`.

Nota: los datos y usuarios Auth del proyecto origen no están incluidos en este paquete. Requieren acceso al proyecto origen para migrarlos.
